<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".FirstPage"
            android:icon="@drawable/aaaa"
            android:label="First Application">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
        <activity android:name=".ApiActivity" android:label="API "> </activity>
        <activity android:name=".ListView"
            android:label="ListView "/>
        <activity android:name=".lec7_1" />
        <activity android:name=".lec7_2" />
        <activity android:name=".login"
            android:label="Curriculum Vitae"/>
        <activity android:name=".MainActivity"
            android:label="Create Curriculum Vitae "/>
        />
    </application>

</manifest>